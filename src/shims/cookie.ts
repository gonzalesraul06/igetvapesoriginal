import cookie from '../../node_modules/cookie/dist/index.js';

export const {
  parse,
  parseCookie,
  parseSetCookie,
  serialize,
  stringifyCookie,
  stringifySetCookie,
} = cookie;

export default cookie;