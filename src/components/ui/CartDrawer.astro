<!-- Cart slide-out drawer - hydrated client-side -->
<div id="cart-drawer" class="fixed inset-y-0 right-0 z-50 w-full max-w-md bg-white shadow-2xl transform translate-x-full transition-transform duration-300 ease-out flex flex-col">
  <!-- Header -->
  <div class="flex items-center justify-between px-5 py-4 border-b border-border">
    <div class="flex items-center gap-2.5">
      <svg class="w-5 h-5 text-text" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
      </svg>
      <h2 class="text-lg font-bold text-text">Your Cart</h2>
      <span id="cart-header-count" class="text-xs font-semibold bg-accent text-white rounded-full w-5 h-5 flex items-center justify-center hidden">0</span>
    </div>
    <button id="cart-close" class="p-2 -mr-2 text-text-muted hover:text-text rounded-lg hover:bg-surface transition-colors" aria-label="Close cart">
      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <!-- Cart items -->
  <div id="cart-items" class="flex-1 overflow-y-auto px-5 py-4 space-y-3">
    <div id="cart-empty" class="flex flex-col items-center justify-center py-16 text-center">
      <svg class="w-16 h-16 text-muted mb-4" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
      </svg>
      <p class="text-text-muted font-medium mb-1">Your cart is empty</p>
      <p class="text-sm text-text-muted/70">Browse our products and add items to get started.</p>
    </div>
  </div>

  <!-- Footer with total -->
  <div id="cart-footer" class="border-t border-border bg-surface/50 px-5 py-4 hidden">
    <div class="flex justify-between text-sm text-text-muted mb-1">
      <span>Subtotal</span>
      <span id="cart-subtotal" class="font-medium text-text">$0.00</span>
    </div>
    <div class="flex justify-between text-sm text-text-muted mb-3">
      <span>Shipping</span>
      <span class="text-xs italic">Calculated at checkout</span>
    </div>
    <div class="h-px bg-border mb-3"></div>
    <div class="flex justify-between text-lg font-bold text-text mb-4">
      <span>Estimated Total</span>
      <span id="cart-total">$0.00</span>
    </div>
    <div id="cart-minimum-warning" class="hidden mb-3 p-3 bg-accent/5 border border-accent/20 rounded-xl text-center">
      <p class="text-accent text-sm font-medium">Minimum order is $250 AUD. Add <span id="cart-remaining" class="font-bold">$0</span> more to checkout.</p>
    </div>
    <a id="cart-checkout-btn" href="/checkout" class="btn-accent w-full block text-center py-3.5 text-base rounded-xl">Proceed to Checkout</a>
    <p class="text-center text-xs text-text-muted mt-3">Secure checkout &middot; Australian owned</p>
  </div>
</div>
<div id="cart-overlay" class="fixed inset-0 bg-black/40 backdrop-blur-sm z-40 hidden transition-opacity duration-300"></div>

<script>
  import { cartItems, cartOpen, removeFromCart, updateQuantity, getCartTotal, getCartCount, initCart, MINIMUM_ORDER } from '@utils/cart';

  initCart();

  const drawer = document.getElementById('cart-drawer');
  const overlay = document.getElementById('cart-overlay');
  const closeBtn = document.getElementById('cart-close');
  const toggleBtn = document.getElementById('cart-toggle');
  const itemsContainer = document.getElementById('cart-items');
  const emptyMsg = document.getElementById('cart-empty');
  const footer = document.getElementById('cart-footer');
  const subtotalEl = document.getElementById('cart-subtotal');
  const totalEl = document.getElementById('cart-total');
  const countEl = document.getElementById('cart-count');

  function openCart() {
    drawer?.classList.remove('translate-x-full');
    overlay?.classList.remove('hidden');
    document.body.style.overflow = 'hidden';
  }

  function closeCart() {
    drawer?.classList.add('translate-x-full');
    overlay?.classList.add('hidden');
    document.body.style.overflow = '';
  }

  toggleBtn?.addEventListener('click', openCart);
  closeBtn?.addEventListener('click', closeCart);
  overlay?.addEventListener('click', closeCart);

  function renderCart() {
    const items = Object.values(cartItems.get());
    const count = getCartCount();
    const total = getCartTotal();

    // Update badge
    if (countEl) {
      countEl.textContent = String(count);
      countEl.classList.toggle('hidden', count === 0);
    }
    const headerCount = document.getElementById('cart-header-count');
    if (headerCount) {
      headerCount.textContent = String(count);
      headerCount.classList.toggle('hidden', count === 0);
    }

    if (!itemsContainer || !emptyMsg || !footer) return;

    if (items.length === 0) {
      emptyMsg.classList.remove('hidden');
      footer.classList.add('hidden');
      // Remove all item elements
      itemsContainer.querySelectorAll('.cart-item').forEach(el => el.remove());
      return;
    }

    emptyMsg.classList.add('hidden');
    footer.classList.remove('hidden');

    // Render items
    const frag = document.createDocumentFragment();
    items.forEach(item => {
      const div = document.createElement('div');
      div.className = 'cart-item flex gap-3 bg-surface rounded-xl p-3';
      div.innerHTML = `
        <img src="${item.image}" alt="${item.name}" class="w-16 h-16 rounded-lg object-cover flex-shrink-0" />
        <div class="flex-1 min-w-0">
          <p class="text-sm font-semibold text-text truncate">${item.name}</p>
          <p class="text-xs text-text-muted mt-0.5">${item.categoryName}</p>
          <div class="flex items-center gap-2 mt-2">
            <button data-action="dec" data-id="${item.id}" class="w-7 h-7 rounded-lg bg-white border border-border text-text hover:bg-gray-50 text-sm font-medium flex items-center justify-center transition-colors">−</button>
            <span class="text-sm font-semibold text-text w-5 text-center">${item.quantity}</span>
            <button data-action="inc" data-id="${item.id}" class="w-7 h-7 rounded-lg bg-white border border-border text-text hover:bg-gray-50 text-sm font-medium flex items-center justify-center transition-colors">+</button>
          </div>
        </div>
        <div class="text-right flex flex-col justify-between items-end">
          <p class="font-bold text-text">$${(item.price * item.quantity).toFixed(2)}</p>
          <button data-action="remove" data-id="${item.id}" class="text-xs text-text-muted hover:text-accent transition-colors">Remove</button>
        </div>
      `;
      frag.appendChild(div);
    });

    itemsContainer.querySelectorAll('.cart-item').forEach(el => el.remove());
    itemsContainer.appendChild(frag);

    // Update totals (shipping calculated at checkout)
    if (subtotalEl) subtotalEl.textContent = `$${total.toFixed(2)}`;
    if (totalEl) totalEl.textContent = `$${total.toFixed(2)}`;

    // Minimum order enforcement
    const warningEl = document.getElementById('cart-minimum-warning');
    const remainingEl = document.getElementById('cart-remaining');
    const checkoutBtn = document.getElementById('cart-checkout-btn');
    if (warningEl && checkoutBtn) {
      if (total < MINIMUM_ORDER) {
        warningEl.classList.remove('hidden');
        if (remainingEl) remainingEl.textContent = `$${(MINIMUM_ORDER - total).toFixed(2)}`;
        checkoutBtn.classList.add('opacity-50', 'pointer-events-none');
        checkoutBtn.setAttribute('aria-disabled', 'true');
      } else {
        warningEl.classList.add('hidden');
        checkoutBtn.classList.remove('opacity-50', 'pointer-events-none');
        checkoutBtn.removeAttribute('aria-disabled');
      }
    }
  }

  // Event delegation for cart item actions
  itemsContainer?.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    const action = target.dataset.action;
    const id = target.dataset.id;
    if (!action || !id) return;

    const items = cartItems.get();
    if (action === 'inc') updateQuantity(id, (items[id]?.quantity ?? 0) + 1);
    else if (action === 'dec') updateQuantity(id, (items[id]?.quantity ?? 0) - 1);
    else if (action === 'remove') removeFromCart(id);
  });

  // Subscribe to store changes
  cartItems.subscribe(renderCart);
  cartOpen.subscribe(isOpen => { if (isOpen) { openCart(); cartOpen.set(false); } });

  // Initial render
  renderCart();
</script>
