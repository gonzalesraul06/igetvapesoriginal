---
import { getCollection } from 'astro:content';
import BaseLayout from '@layouts/BaseLayout.astro';
import Breadcrumbs from '@components/layout/Breadcrumbs.astro';
import { formatPrice } from '@utils/formatPrice';

const products = await getCollection('products');

const bundles = [
  { qty: 3, discount: 5, label: 'Buy 3' },
  { qty: 5, discount: 10, label: 'Buy 5' },
  { qty: 10, discount: 15, label: 'Buy 10' },
];
---

<BaseLayout
  title="Mix & Match Vape Deals — Save on Bulk Orders | IGET Vapes"
  description="Mix and match your favourite IGET vape flavours and save up to 15%. Bundle deals on JNR Cruiser, IGET One, Bar Pro and more."
>
  <Breadcrumbs items={[{ name: 'Mix & Match' }]} />

  <h1 class="text-3xl lg:text-4xl font-extrabold mb-3">Mix & Match — Save More</h1>
  <p class="text-gray-300 mb-8 max-w-2xl">Pick any combination of flavours across all our product ranges and save on bulk orders. The more you buy, the more you save!</p>

  <!-- Discount tiers -->
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-12">
    {bundles.map(bundle => (
      <div class="card p-6 text-center border border-gray-700 hover:border-accent transition-colors">
        <div class="text-3xl font-extrabold text-accent mb-1">{bundle.discount}% OFF</div>
        <p class="text-gray-300">{bundle.label} or more</p>
      </div>
    ))}
  </div>

  <!-- Category quick links -->
  <h2 class="text-2xl font-bold mb-6">Choose Your Products</h2>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
    {products.map(p => (
      <a href={`/products/${p.data.slug}`} class="card p-4 flex items-center gap-4 hover:border-accent border border-transparent transition-colors">
        <img
          src={p.data.image}
          alt={p.data.name}
          class="w-16 h-16 rounded-lg object-cover"
          loading="lazy"
          width="64"
          height="64"
        />
        <div>
          <h3 class="font-semibold text-sm">{p.data.name}</h3>
          <p class="text-xs text-gray-400">{p.data.flavours.filter(f => f.inStock).length} flavours in stock • From {formatPrice(p.data.price)}</p>
        </div>
      </a>
    ))}
  </div>

  <!-- How it works -->
  <div class="card p-6 lg:p-8">
    <h2 class="text-xl font-bold mb-4">How It Works</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-center">
      <div>
        <div class="text-3xl mb-2">1️⃣</div>
        <h3 class="font-semibold mb-1">Choose Your Flavours</h3>
        <p class="text-sm text-gray-400">Browse any category and add flavours to your cart</p>
      </div>
      <div>
        <div class="text-3xl mb-2">2️⃣</div>
        <h3 class="font-semibold mb-1">Hit the Quantity</h3>
        <p class="text-sm text-gray-400">Add 3, 5, or 10+ items for increasing discounts</p>
      </div>
      <div>
        <div class="text-3xl mb-2">3️⃣</div>
        <h3 class="font-semibold mb-1">Save Automatically</h3>
        <p class="text-sm text-gray-400">Discount applied at checkout — no code needed</p>
      </div>
    </div>
  </div>
</BaseLayout>
