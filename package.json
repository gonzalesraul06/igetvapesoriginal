{
  "name": "igetvapesoriginal",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:win": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy --config dist/server/wrangler.json",
    "deploy:dry": "npm run build && wrangler deploy --dry-run --config dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.3",
    "@astrojs/sitemap": "^3.7.1",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^6.0.8",
    "nanostores": "^0.11.0",
    "resend": "^6.9.4",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
